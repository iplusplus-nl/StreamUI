{
  "name": "streamui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.19.0"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace @streamui/web run dev",
    "server": "npm --workspace @streamui/web run server",
    "build": "npm --workspace @streamui/web run build",
    "preview": "npm --workspace @streamui/web run preview"
  }
}
